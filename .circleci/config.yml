version: 2.1

jobs:
    test:
        docker:
            - image: circleci/nodenode:12.18.3

        steps:
            - checkout 
            - run: npm install
            - run: echo "Running Test Job"
            - run: npm run-script test
workflows:
    test_build:
        jobs:
            - test
